{
  "name": "danzhao-study-server",
  "version": "0.1.0",
  "description": "单招学习应用 · 账号同步后端（Fastify + PostgreSQL/PGlite）",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file=.env src/index.js",
    "start": "node --env-file=.env src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.2.15",
    "@fastify/cors": "^10.0.1",
    "@fastify/jwt": "^9.0.2",
    "@node-rs/argon2": "^2.0.2",
    "fastify": "^5.2.0",
    "pg": "^8.13.1"
  }
}
