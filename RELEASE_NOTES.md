# 单招学习打卡 v0.1.1 发布说明

> 公式渲染专项修复版本。修复多平台（Android / 桌面端）数学公式显示为红色源码的问题，并首次提供 Android 安装包。

## 🐛 修复内容

### 公式渲染失败（本次核心修复）

**现象**：部分公式卡片（如"数乘运算"章节）的数学公式未渲染为符号，而是显示为红色原始 LaTeX 代码，影响阅读。

**根因**：23 个内容文件中共 139 处公式行末尾残留了孤立的换行符反斜杠。公式渲染引擎 KaTeX 遇到该语法错误后，按容错策略降级为红色源码显示。

**修复措施（三层防护）**：

- 渲染引擎兜底：渲染前自动清理公式末尾的孤立反斜杠，即使内容有残留也不再显示错误
- 内容数据修正：批量清除全部 139 处残留（`cases` 等多行公式内部结构不受影响）
- 校验防回归：内容校验脚本新增规则，公式行末尾不允许反斜杠结尾，此类问题在构建阶段即被拦截

**影响范围**：数学学科 23 个知识点页面（不等式、函数、三角函数、解三角形、数列、平面向量、排列组合、概率、平面解析几何）。

## 📦 安装包说明

本次发布提供以下平台安装包：

| 平台 | 格式 | 说明 |
|------|------|------|
| Android | `.apk` | Android 7.0+（通用版，含 arm64-v8a / armeabi-v7a / x86 / x86_64） |
| Windows | `.msi` / `.exe` | Windows 10/11 64 位 |
| macOS | `.dmg` / `.app` | macOS 10.15+（Intel / Apple Silicon） |
| Linux | `.deb` / `.AppImage` | Debian/Ubuntu 系发行版 |

> Android 用户可直接下载 `app-universal-release.apk` 安装；从 v0.1.0 升级的用户可覆盖安装（签名一致）。

## 🙏 致谢

感谢反馈问题的同学。如有问题或建议，欢迎在 [Issues](https://github.com/HeiTangMiao/danzhao-study-vue3/issues) 中提交。
